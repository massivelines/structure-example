// inside test folder include global tests that don't have a folder
// example: integration testing where you load multiple components together, testing form and inputs together

test('Test holder', () => {
  expect(1 + 1).toBe(2);
});
